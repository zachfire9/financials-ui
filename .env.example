VITE_FINANCIALS_API_PROXY_TARGET=http://localhost:8080
VITE_FINANCIALS_API_BASE_URL=/api

# Session mode options:
# - persistent: financial item CRUD uses the API repository
# - ephemeral: import/export JSON stays browser-owned; projections send current items in request body
VITE_FINANCIALS_SESSION_MODE=persistent
